# -----------------------
# Backend
# -----------------------

PULSE_ENV=dev
PULSE_API_PORT=8000

# Internal Pulse DB
PULSE_DB_HOST=localhost
PULSE_DB_PORT=5433
PULSE_DB_NAME=pulse
PULSE_DB_USER=pulse_user
PULSE_DB_PASSWORD=changeme

# Security
JWT_SECRET_KEY=changeme
JWT_EXPIRATION_MINUTES=60

# Email alerts
EMAIL_PROVIDER=smtp
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=changeme
SMTP_PASSWORD=changeme
ALERT_FROM_EMAIL=alerts@pulse.dev

# Safety limits
MAX_QUERIES_PER_MINUTE=30
QUERY_TIMEOUT_SECONDS=2
REPLICA_LAG_THRESHOLD_SECONDS=60

# -----------------------
# Frontend
# -----------------------

VITE_API_BASE_URL=http://localhost:8000